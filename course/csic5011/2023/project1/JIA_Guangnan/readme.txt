The python codes are to get the figures in the report. For example, running fig1.py will get the Figure 1 in the report. 

Before running the code, make sure you are in the right environment setup and have installed all the necessary packages. To achieve this, you can run the following command in the terminal:
     conda env create --file project1.yml

Where project1.tml is attached in the zip also.
 